import { expect } from 'chai';
import { ValidationRule } from '@core/policy/engine/ValidationRule';

describe('ValidationRule', () => {
  it('should be properly defined', () => {
    expect(ValidationRule).to.exist;
  });
  
  // TODO: Add more specific tests
});
