import { expect } from 'chai';
import { EnvironmentType } from '@core/policy/types/EnvironmentType';

describe('EnvironmentType', () => {
  it('should be properly defined', () => {
    expect(EnvironmentType).to.exist;
  });
  
  // TODO: Add more specific tests
});
