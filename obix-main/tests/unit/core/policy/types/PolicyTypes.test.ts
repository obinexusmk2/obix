import { expect } from 'chai';
import { PolicyTypes } from '@core/policy/types/PolicyTypes';

describe('PolicyTypes', () => {
  it('should be properly defined', () => {
    expect(PolicyTypes).to.exist;
  });
  
  // TODO: Add more specific tests
});
